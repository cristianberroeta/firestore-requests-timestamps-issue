{
  "name": "firestore-requests-timestamps-issue",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build-watch & firebase --project demo-firestore-requests-timestamps-issue emulators:start",
    "build-watch": "webpack --watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase": "^9.0.1"
  },
  "devDependencies": {
    "webpack": "^5.52.0",
    "webpack-cli": "^4.8.0"
  }
}
